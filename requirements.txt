# Core
pydantic-ai>=1.0.0
pydantic>=2.7.0

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# HTTP client (OWUI API + sub_agent self-calls)
httpx>=0.27.0

# MCP transport (pydantic-ai pulls mcp, but pin for stability)
mcp>=1.6.0
